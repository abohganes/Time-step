# Expo HAS CHANGED

This project targets Expo SDK 54 (downgraded from the SDK 57 scaffold default because
Expo Go for SDK 57 was still pending Play Store approval). Read the exact versioned
docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
